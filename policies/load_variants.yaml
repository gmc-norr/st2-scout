---
name: load_variants.concurrency
description: Limits the concurrent load into loqusdb to one case at a time
enabled: true
resource_ref: scout.load_variants
policy_type: action.concurrency
parameters:
  action: delay
  threshold: 1